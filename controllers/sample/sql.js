const CREATE_EMPLOYEE_TABLE =
  "CREATE TABLE IF NOT EXISTS employees (id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(100), email VARCHAR(120))";

const GET_ALL_EMPLOYEES = "SELECT * FROM employees";

const ADD_EMPLOYEE = "INSERT INTO employees SET ?";

module.exports = {
  ADD_EMPLOYEE,
  CREATE_EMPLOYEE_TABLE,
  GET_ALL_EMPLOYEES,
};
